#include <stdio.h>
//#include <sys/socket.h> // ignora e testa su unix
#include "utils.h"

int main(int argc, char* argv[]) {
    fprintf (stdout, "Collector creato\n");
    fflush(stdout);
    return 0;
} //?